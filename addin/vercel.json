{
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build -w addin",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://www.silkview.org/api/:path*"
    }
  ]
}
