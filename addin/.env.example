# Next.js API (production: https://www.silkview.org or your web URL)
VITE_API_URL=http://localhost:4003

# Auth dialog target — defaults to VITE_API_URL when set (required in production;
# add-in host is static and has no /auth/excel route)
# VITE_OFFICE_AUTH_ORIGIN=https://www.silkview.org
